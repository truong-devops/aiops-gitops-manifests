# Scripts

Thư mục này dành cho script hỗ trợ GitOps sau này, ví dụ:

- Cập nhật image tag theo commit SHA.
- Render Kustomize để kiểm tra trước khi merge.
- Smoke test sau sync.
- Tạo GitOps MR từ pipeline.
