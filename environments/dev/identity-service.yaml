apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity-service
  labels:
    app.kubernetes.io/name: identity-service
    app.kubernetes.io/part-of: aiops-video-platform
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: identity-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: identity-service
        app.kubernetes.io/part-of: aiops-video-platform
    spec:
      containers:
        - name: identity-service
          image: harbor.local/aiops/identity-service:dev-placeholder
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
---
apiVersion: v1
kind: Service
metadata:
  name: identity-service
  labels:
    app.kubernetes.io/name: identity-service
    app.kubernetes.io/part-of: aiops-video-platform
spec:
  selector:
    app.kubernetes.io/name: identity-service
  ports:
    - name: http
      port: 80
      targetPort: http
